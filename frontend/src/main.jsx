import React from 'react'
import ReactDOM from 'react-dom/client'
import MultiUserApp from './MultiUserApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MultiUserApp />
  </React.StrictMode>,
)